import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    display: 'flex',
    flexDirection: 'row',
    width: '100%',
    alignItems: 'center',
    marginBottom: 20,

  },
  container: {
    alignItems: "center", 
    marginTop: 5, 
    padding: 10
  }
})